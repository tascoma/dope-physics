export function Footer() {
  return (
    <footer
      style={{
        padding: "40px 0",
        borderTop: "1px solid rgba(29,31,32,.1)",
        fontSize: 13,
        lineHeight: "24px",
        color: "rgba(29,31,32,.7)",
      }}
    >
      Dope Physics Engine · point-mass exterior ballistics · figures are model output, not range data.
    </footer>
  );
}
